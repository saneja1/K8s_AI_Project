apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy-3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-deploy-3
  template:
    metadata:
      labels:
        app: nginx-deploy-3
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
